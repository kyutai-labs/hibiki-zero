# Inference Server

The inference server hosts the modified Hibiki-Zero model as a FastAPI WebSocket service. It receives audio from the telephony gateway, streams translated audio back, and manages speaker embedding and voice transfer.